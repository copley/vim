These commands remove the .srt file config. 
:g/^\d\+$/d
:g/^\(\d\+\|[0-9:, \->]\+\)$/d
:g/^$/d

This command removes the last lines.
:40001,$w new_file.txt

:g/^$/d    // bunches all the lines up.

Paste the clipboard content into vim from a windows11 pc instance.
"+p


Macbook Textfield Insert. Can’t get out of insert.
1. Try Pressing the "fn" or "Insert" Key
Macs don’t have a dedicated Insert key, but some keyboards map it to Fn + Enter or Fn + M.
Try pressing Fn + M to toggle between insert and overwrite modes.


Testing added line here.
